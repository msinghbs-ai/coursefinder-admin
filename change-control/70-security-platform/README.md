# 70 — Security / Platform

Use this category for ACL/RBAC, authentication/authorisation, RLS, secrets, worker/runtime, hosting, platform and security-control changes.

Security changes must record bounded permission/negative tests and rollback paths.