# M2.4.6 NEXT CHAT

## Active baseline — 15 September 2026

Start from repository/runtime truth. Follow `PROJECT_INSTRUCTIONS.md`, `docs/README.md`, then `project-runsheets/milestone-2/m2.4/m2.4.6/EXECUTION-PRIORITY.md`.

Accepted predecessor baseline:

- M2.4.5 / CF-245: CLOSED / PASS / FROZEN;
- Pilot baseline: `e62c01cadaf43efa8c3d8ea57625c23874d1b010`;
- Pilot Supabase: `fxcwkweaxjtknorudmwp`;
- visible accepted release: v2.15.79 / package 0.1.6;
- Production is not provisioned; M2.5 remains paused until M2.4.9 GO.

## Standing execution priority

Do not turn M2.4.6 into recurring logic redesign. A new issue blocks only if it directly prevents safe deterministic admission, security/identity/Layer authority, Search/API correctness or recovery from an active failure. Record all other improvements as later follow-ups and continue.

Admission and website handover take priority over non-blocking orchestration refinements.

## Runtime progress already completed

Existing accepted CF-245 admission functions were reused:

- 272 latest qualified official-URL candidates checked;
- 269 applied;
- 122 canonical changes;
- Search projection applied 122 changed rows;
- official-course-URL website/Search coverage is now **527**.

Website v3.1 runtime verification (`has_link=true`) returns total **527** and exposes official URL, regulatory tuition, intake and English summaries.

Observed-artifact replay is currently exhausted for deterministic intake/English additions: 0 new intake candidates / 0 new English candidates. The remaining 457 provider-current-tuition candidates are explicitly Layer 3 fee-validation backlog and do not block handover.

## Exact continuation sequence

1. Run one bounded qualified AU operational exercise using the existing dispatcher/admission path.
2. Fix only a defect that demonstrably blocks that bounded exercise or safe admission.
3. Do not reopen intake/English replay logic; it is exhausted for current Evidence.
4. Do not make the 457 Layer 3 fee candidates a prerequisite for current website handover.
5. If the bounded exercise is green, close/freeze M2.4.6 and move immediately to M2.4.7 controlled scale.
6. Website consumer handover proceeds in parallel using the current v3.1 contract and admitted dataset.

Do not reopen CF-093. Do not weaken source qualification, Evidence, Layer 4, security or Search admission boundaries to increase counts.
