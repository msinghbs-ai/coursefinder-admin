# M2.4.7 NEXT CHAT

Start from repository/runtime truth. Follow `PROJECT_INSTRUCTIONS.md`, `docs/README.md`, `docs/coursefinder-end-to-end-automated-data-admission-roadmap-v1.0.md`, `CF-CHG-20260915-247`, then this directory.

## Accepted predecessor

- M2.4.6 / CF-246: CLOSED / PASS / FROZEN;
- accepted Pilot: `90ddbdf28bfad57eb8d5a0ede1b8e506e252908a`;
- Pilot Supabase: `fxcwkweaxjtknorudmwp`;
- Production remains unprovisioned; M2.5 remains paused until M2.4.9 GO.

## Programme objective

Do not treat scraper/parser completion or a `layer3_required` status as feature completion. The active outcome is:

`Source → L1 authority → L2 acquisition/Evidence/deterministic extraction → automatic L3 interpretation → deterministic admission or L4 exception → Search/API projection → live Admin telemetry`.

Existing accepted components are retained. Do not big-bang refactor working parsers/functions.

## Runtime proof of the gap

RMIT controlled wave request `f120fb4b-ec69-4f2c-8147-550a338c6f3d`, batch `13f33fcf-05b9-4379-b214-cf4760bbe1f3`:

- 25/25 processed;
- 0 resolved L2;
- 25 `layer3_required`;
- 0 blocked;
- USD 0 provider cost.

Deployed `layer2-batch-runner` stops at `layer3_required`; it does not automatically dispatch general Layer 3 Course work. Deployed `layer3-interpret` is a valid single-item Evidence interpreter, but the general Layer 3 UI invokes it manually through **Run eligible interpretation**.

The tuition-specific OpenRouter free profile remains paused because its benchmark failed; do not unpause or reuse an unrelated benchmarked model task class simply for throughput.

## Consumer baseline

- 33,105 Search courses;
- 527 official-course URLs;
- 487 intake coverage;
- 520 English coverage;
- 161 provider-current tuition;
- website v3.1 `has_link=true` total 527.

## Exact continuation sequence

1. Implement additive durable Layer 3 queue/state contract from eligible Layer 2 fall-out.
2. Implement server-owned Layer 3 dispatcher using existing `layer3-interpret`, with idempotency, benchmarked model selection, concurrency/RPM/day/cost limits, retries and stale recovery.
3. Add/benchmark the actual Course task classes required by current fall-out; free OpenRouter routes are preferred only when they pass task-specific quality gates.
4. Implement deterministic post-L3 admission policy; AI must not receive unrestricted canonical-write authority.
5. Trigger governed Search/API projection only after accepted canonical changes.
6. Add live Admin end-to-end progress showing L2/L3/L4/admitted/remaining, Evidence, calls/tokens/cost/latency and exact blockers.
7. Re-run bounded AU wave and prove admitted-data delta without chat/operator intervention.
8. Apply the same engine to an independently qualified NZ pilot and qualified CA pilot.
9. Run GB/US/IE/DE adapter portability fixtures, then one qualified live pilot per country when source authority is accepted.
10. Converge Scholarship/Ranking/Statistics operations onto the same job/telemetry/control-plane conventions without flattening their source-specific semantics.

Do not launch larger Layer 2 waves simply to accumulate more Layer 3 backlog. Bugs/security/tooling can proceed in parallel; they block only when they affect authority, correctness, recoverability, secure execution or consumer integrity.
