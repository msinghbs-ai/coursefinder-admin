# M2.4.7 NEXT CHAT

Start from repository/runtime truth under `PROJECT_INSTRUCTIONS.md`, `docs/README.md`, CF-CHG-20260915-247 and this directory. M2.4.7 remains Gate D; no larger L2 wave is authorised until bounded L3 drain is proven.

## Current critical-path truth

Pilot PR #99 branch `cf-247-candidate-bound-layer3` exact head is **`56afd9466c7375dc0a5f2c83d6cd58dbbac02715`**. Exact-head Pilot Frontend Build **35224454671 / #2452 PASS**. The service-owned interpreter and durable `layer3-work-dispatch` are repository-owned and deployed; existing curator/manual `layer3-interpret` remains unchanged.

Qualification remains fail-closed. Latest trusted candidate benchmark request 6326 remains provider 0/4, controls 4/4, 9 calls, 12,807 input + 1,247 output tokens, USD 0, max 10.133s. Diagnostic 6327 established the prior UQ snapshots do not explicitly support the governed annual/indicative-annual basis adjacent to the candidate amount.

Fresh corpus reconciliation materially narrows the correction: runtime currently has **only one** active international `provider_current_tuition` row with retained Evidence and explicit `basis='annual'`: RMIT Associate Degree in Business, AUD 37,440, fee year 2027, Evidence `02f76986-f7f6-4b7c-9671-36b67a9c4969`. The current benchmark corpus function orders latest admitted rows and therefore selects UQ `indicative_annual` rows first. Do not weaken validation to make those UQ rows pass.

The RMIT official course page independently states the international fee as **AU$37,440 (2027 annual)**, so this row is a legitimate explicit-basis positive candidate. However, one valid positive is insufficient for the current 3–6 positive benchmark requirement. The next executable approach is therefore to build/capture at least three governed retained-Evidence positive cases whose first-party Evidence itself explicitly states amount + international audience + year + annual/indicative-annual basis, rather than treating canonical basis alone as proof. Prefer already-governed RMIT/provider evidence where available; if new Evidence acquisition is required, keep it bounded to qualification corpus creation and preserve Evidence lineage/identity.

Latest trusted lifecycle baseline remains 2,511 `layer3_required` / 2,423 `resolved_l2` / 815 cancelled / 10 blocked; L3 queue 0; Evidence 30,925 until refreshed. **NO DATA ADMISSION PROVEN**.

## Exact continuation sequence

1. Obtain at least three explicit-basis retained-Evidence benchmark positives. Start with the existing RMIT AD029 annual row/Evidence and locate or acquire two additional first-party explicit annual/indicative-annual cases. Do not relabel unsupported UQ snapshots.
2. Add a forward-only benchmark-corpus correction that selects only Evidence-qualified explicit-basis positives; repository-back and apply it. Do not rewrite the already-applied corpus migration.
3. Run exactly one governed semantic+safety qualification. Require full provider + control PASS before unpausing tuition.
4. On PASS, immediately enqueue/dispatch only 10–25 existing Evidence-backed tuition items through `layer3-work-dispatch` and capture M247-FU-021 proof across L2 before/after, L3 create/reserve/result, deterministic admission/L4, canonical delta, Search/API projection/no-op, Evidence lineage, failures/retries, calls/tokens/cost/latency and resource/quota headroom.
5. Without that proof report **NO DATA ADMISSION PROVEN**. Normal AU/NZ admission scheduling resumes only after bounded end-to-end drain is demonstrated.

M247-FU-020/021/022 and the anti-loop rule remain mandatory.
