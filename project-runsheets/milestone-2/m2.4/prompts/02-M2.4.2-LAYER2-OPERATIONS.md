# Prompt — M2.4.2 Layer 2 Enrichment Operations

---

**M2.4.2 — LAYER 2 FULL ENRICHMENT, OPERATIONS MATURITY & PERFORMANCE**

Start only after M2.4.1 is CLOSED/PASS.

Read `PROJECT_INSTRUCTIONS.md`, M2 Standing Instructions, A1–A6, latest M2.4 current state/follow-ups, the M2.4.2 runsheet, current Change Controls, current Pilot/runtime/Supabase/provider state and CI evidence.

Objective: operate Layer 2 as one routine enrichment service, execute broad accepted AU enrichment, measure throughput/economics/storage/completeness/fall-out, and keep NZ first-party L2 explicitly deferred until source qualification is accepted.

Normal navigation must use `Data Operations → Layer 2 — Enrichment`; permanent tests must use shared navigation adapters and never resurrect floating launchers or obsolete menu labels.

Implement in bounded slices: routing/provider controls → queue/batch/progress → broad enrichment → Evidence/candidate consequence → scheduling/rechecks/cleanup → performance/economics → UX tuning from evidence.

Use A1 for every slice: targeted validation first, bounded integration second, one full deployed acceptance matrix only for the nominated sub-milestone candidate. Do not repeatedly launch the complete matrix during tuning.

Measure at minimum provider success, deterministic resolution, paid fallback, units/cost, throughput, Evidence growth, DB/API/Edge/browser latency, retries/throttles and Layer 2→3 fall-out. Preserve Search/Publication boundaries and no secret exposure.

Record blocked/noncritical issues in FOLLOW-UPS and continue independent safe tasks. Update Guides/Runbooks/release notes as behaviour evolves.

Close only when Layer 2 can be run, monitored, resumed and measured as a production-shaped service and the UI/documentation reflects actual broad-run evidence.

---
